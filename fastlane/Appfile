# Fastlane Appfile для CloudMusicPlayer
app_identifier("com.samvel.CloudMusicPlayer")
apple_id(ENV["APPLE_ID"] || "developer@example.com")
itc_team_id(ENV["ITC_TEAM_ID"]) if ENV["ITC_TEAM_ID"]
team_id(ENV["TEAM_ID"]) if ENV["TEAM_ID"]
